# Virtualization Notes

Research virtualization and cloud-provider behavior at the architecture level.

Topics include CPU allocation, memory overcommit, storage backends, virtual networking, images, snapshots, provisioning APIs, lifecycle operations, and failure handling.

Provider comparisons should document the test conditions and avoid turning one environment into a universal claim.
